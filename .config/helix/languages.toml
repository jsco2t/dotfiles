#
# golang
# 
[language-server.gopls.config]
gofumpt = true
# language-servers = ["gopls", "golangci-lint2-langserver"]

# [language-server.golangci-lint2-langserver]
# command = "golangci-lint-langserver"

# [language-server.golangci-lint2-langserver.config]
# command = [
#   "golangci-lint",
#   "run",
#   "--output.json.path",
#   "stdout",
#   "--show-stats=false",
#   "--issues-exit-code=1",
# ]

[[language]]
name = "go"
auto-format = true
formatter = ["gofumpt"] # gofmt would also work

#
# markdown
# 
[language-server.harper-ls]
command = "harper-ls"
args = ["--stdio"]

# does not seem to add value over `formatter implementation below`
#[language-server.dprint-ls]
#command = "dprint"
#args = ["lsp"]

[[language]]
name = "markdown"
language-servers = ["marksman", "harper-ls"]
formatter = { command = "dprint", args = ["fmt", "--stdin", "md"] }
auto-format = true
